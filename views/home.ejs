<%- include('partials/header') -%> 

    <h1>HOME</h1>
    <p> <%= homeStartingContent %> </p>

    <% posts.forEach(function(post) { %>
        <h1><%= post.tittle %></h1>

        <p><%= post.text.substring(0,100) %> ... <a href="posts/<%= post.tittle %>">read more</a> </p> 
     <% }); %> 
    
    
<%- include('partials/footer') -%>

